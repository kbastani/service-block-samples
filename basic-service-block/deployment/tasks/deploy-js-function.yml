---
platform: linux
image_resource:
  type: docker-image
  source:
    repository: alpine
    tag: '3.4'
inputs:
- name: release
run:
  path: sh
  args:
  - -exc
  - |
    cd release \
    && ./deploy-function.sh $bucket_name